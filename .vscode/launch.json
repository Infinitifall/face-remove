{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/face-remove",
            "args": [],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "make debug"
        }
    ]
}